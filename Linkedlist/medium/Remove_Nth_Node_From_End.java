package com.Linkedlist.medium;

public class Remove_Nth_Node_From_End {
    /*
    public static Node removeKthNode(Node head, int k)
    {
        //    Step 1-->Calculate Size
        Node temp=head;
        int size=0;
        while(temp!=null){
            size++;
            temp=temp.next;
        }
        // Step 2 Check Tumko head To nhi Delete Karna hai
        if(size==k){
            head=head.next;
            return head;
        }
        //Step 3 Go DeletePosition -1
        int delPos=size-k;
        int count=1;
        temp=head;
        while(count!=delPos){
            count++;
            temp=temp.next;
        }
        //Finally Delete
        temp.next=temp.next.next;
        return head;
    }

     */
    public static void main(String[] args) {

    }
}
