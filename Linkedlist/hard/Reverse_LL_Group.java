package com.Linkedlist.hard;

public class Reverse_LL_Group {
    /*
    public static Node reverse(Node head, int k)
    {

        Node curr=head;
        Node prev=null;
        Node next=null;
        int count=0;
        while(curr!=null && count<k){
            next=curr.next;
            curr.next=prev;
            prev=curr;
            curr=next;
            count++;
        }
        if(next!=null)
            head.next=reverse(next,k);
        return prev;
    }

     */
    public static void main(String[] args) {

    }
}
